[package]
name = "agenttrace-py"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "_core"
crate-type = ["cdylib"]

[dependencies]
agenttrace-core = { path = "../agenttrace-core" }
pyo3 = { version = "0.20", features = ["extension-module"] }
serde_json = "1.0"
pythonize = "0.20"
